name: Supervisor Closing (15:50 WIB)

on:
  schedule:
    - cron: '50 8 * * 1-5'  # 15:50 WIB = 08:50 UTC, Senin–Jumat
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 45

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Restore agent cache
        uses: actions/cache@v4
        with:
          path: data/agent_cache.json
          key: ihsg-cache-${{ github.run_id }}
          restore-keys: |
            ihsg-cache-

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Supervisor Closing Scan
        env:
          LLM_PROVIDER:        ${{ secrets.LLM_PROVIDER }}
          GROQ_API_KEY:        ${{ secrets.GROQ_API_KEY }}
          GEMINI_API_KEY:      ${{ secrets.GEMINI_API_KEY }}
          TELEGRAM_BOT_TOKEN:  ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:    ${{ secrets.TELEGRAM_CHAT_ID }}
        run: python run_job.py --job supervisor
